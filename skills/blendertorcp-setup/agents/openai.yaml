interface:
  display_name: "BlenderToRCP Setup"
  short_description: "Configure the BlenderToRCP CLI — locate Blender, verify the plugin, set up a shell alias."

policy:
  allow_implicit_invocation: false
